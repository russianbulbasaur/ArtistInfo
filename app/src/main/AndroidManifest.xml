<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="russian.bulbasaur.artistinfo">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArtistInfo">
        <activity
            android:name=".display_dis"
            android:label=""
            android:theme="@style/Theme.ArtistInfo.NoActionBar"></activity>
        <activity
            android:name=".gernerator"
            android:label=""
            android:theme="@style/Theme.ArtistInfo.NoActionBar" />
        <activity
            android:name=".scan"
            android:label="Scan Your QR"
            android:theme="@style/Theme.ArtistInfo.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>